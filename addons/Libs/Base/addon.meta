<rubble:addonmeta
	lib=true
	header=`All the base templates and other such stuff, critical to normal operation.`
	
	description=`
Almost all the other addons require this one, so it is almost always active.
See "Rubble Base Template" for documentation.
`

	# DO NOT under ANY circumstances add an "activates" key to this addon!
	# As EVERY other addon depends on this one, doing so will cause an infinite
	# activation loop!
>